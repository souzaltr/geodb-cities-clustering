version: "3.9"

services:
  backend:
    build: ./server
    ports:
      - "3001:3001"
    volumes:
      - ./server:/app
    restart: always

  frontend:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
    depends_on:
      - backend
    restart: always
